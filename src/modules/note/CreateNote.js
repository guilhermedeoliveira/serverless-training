import React from 'react';

export const CreateNote = () => (
  <div>
    <form>
      <input type="text" placeholder="Write your note" />

      <button type="sumit">Add Note</button>
    </form>
  </div>
);

export default CreateNote;
